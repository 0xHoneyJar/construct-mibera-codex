---
title: Mibera Codex
description: The grimoire MCP — anti-hallucination lookup over canonical Mibera lore. Read by agents, browsed by humans, tools shared between.
---

import { GrimoireShelf } from '../components/grimoire-shelf'

<img
  src="/og.png"
  alt="MICODEX — The Grimoire MCP"
  style={{ width: "100%", maxWidth: "880px", height: "auto", display: "block", margin: "1rem auto 2.5rem", border: "1px solid var(--nier-border-medium)" }}
/>

# the codex

a row of grimoires. each one tends a slice of the Mibera world. zones, archetypes, factors, grails, miberas.

the dance returns. the codex was already here, watching. agents read these books to stay grounded. humans browse them for the same reason.

read-only. canonical. eight tools. one [mcp 2025-06-18](https://modelcontextprotocol.io/specification/2025-06-18) endpoint.

<GrimoireShelf books={[
  { n: 1, label: "lookup_zone",            hint: "zone by slug",         href: "/tools/lookup_zone" },
  { n: 2, label: "lookup_archetype",       hint: "one of four",          href: "/tools/lookup_archetype" },
  { n: 3, label: "lookup_factor",          hint: "factor id → lore",     href: "/tools/lookup_factor" },
  { n: 4, label: "lookup_grail",           hint: "1/1 grail",            href: "/tools/lookup_grail" },
  { n: 5, label: "lookup_mibera",          hint: "one of ten thousand",  href: "/tools/lookup_mibera" },
  { n: 6, label: "list_zones",             hint: "discovery",            href: "/tools/list_zones" },
  { n: 7, label: "list_archetypes",        hint: "the four",             href: "/tools/list_archetypes" },
  { n: 1, label: "validate_world_element", hint: "the keystone",         href: "/tools/validate_world_element" },
]} />

## the eight

```
lookup_zone(slug)            → zone entry, lynch primitives, KANSEI tokens
lookup_archetype(name)       → era, locations, figures, fashion
lookup_factor(factor_id)     → score-mibera ↔ codex lore
lookup_grail(query)          → 1/1 grail by id, slug, or name
lookup_mibera(id)            → full canonical trait set (1–10000)
list_zones()                 → every canonical zone
list_archetypes()            → the four
validate_world_element(t,v)  → fuzzy match + coverage-gap log
```

## who reads this

- [`freeside-characters`](https://github.com/0xHoneyJar/freeside-characters) — discord narrative bots (ruggy, satoshi). first production consumer.
- operator harnesses — claude desktop, claude code, chatgpt, custom mcp clients.
- you, after [quickstart →](/quickstart)

## what the codex doesn't do

- it doesn't write. the codex repo is the only writer.
- it doesn't talk. it returns json. your agent makes prose.
- it doesn't police voice. only [HARD-tier](/anti-hallucination) claims must look up; everything else stays open.
